public class TestMax {
   /** 主方法 */
   public static void main (String[]  args, String message, int n) {
      int i = 5;
      int j = 2;
      int k = max(i, j);
      System.out.println( i + " 和 " + j + " 比较，最大值是：" + k);
   }
 

   public static int max(list<    int[]         , map<string, int>> num1, int num2) {
      int result;
      if (num1 > num2)
         result = num1;
      else
         result = num2;
 
      return result; 
   }
      /**
       * description: 
       * param {double...} numbers
       * param {int} num2
       * return {*}
       */      
      public static int max2(double... numbers, int num2) {
          
   }
   
}

// 一个简单的构造函数
class MyClass {
  int x;
 
  // 以下是构造函数
  MyClass(int i ) {
    x = i;
  }
}